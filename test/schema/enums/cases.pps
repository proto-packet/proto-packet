// An enum with a single case.
enum SingleCase {

    // The single case.
    One = 1;
}

// An enum with multiple cases.
enum MultiCase {

    // The first case.
    One = 1;

    // The second case.
    Two = 2;

    // The third case.
    Three = 3;
}
