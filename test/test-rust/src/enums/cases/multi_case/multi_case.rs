/// An enum with multiple cases.
#[derive(Copy, Clone, Ord, PartialOrd, Eq, PartialEq, Hash, Debug)]
pub enum MultiCase {
    /// The first case.
    One,
    
    /// The second case.
    Two,
    
    /// The third case.
    Three,
}
